import Account from '@/components/Account'
import React from 'react'

export const metadata = {
    title: "CadexLaw ⋅ Account",
};

export default function page() {
    return (
        <Account />
    )
}
